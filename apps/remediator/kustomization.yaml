apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - cronjob.yaml
configMapGenerator:
  - name: remediator-code
    files:
      - remediator.py
      - requirements.txt
generatorOptions:
  disableNameSuffixHash: true
